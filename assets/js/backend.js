$(function () {
	      $('#tglmulai').datetimepicker({
	          format: "YYYY-MM-DD",
	      });
	      $('#tglselesai').datetimepicker({
	          format: "YYYY-MM-DD",
	      });
	      $('#jmmulai').datetimepicker({
	          format: "HH:mm",
	      });
	      $('#jmselesai').datetimepicker({
	          format: "HH:mm",
	      });

	      $('#tgpost').datetimepicker({
	          format: "YYYY-MM-DD HH:mm:ss",
	      });
	  });

